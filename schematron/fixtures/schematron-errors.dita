<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "task.dtd">
<task id="schematron-errors-catalog">
  <title>Installing The Product On Your System.</title>
  <!-- HeadingPunctuation: trailing period -->
  <!-- Headings: title-case "The", "On", "Your" -->
  <shortdesc>This section describes how to install the product on a RHEL system.</shortdesc>
  <!-- SelfReferentialText: "This section" -->
  <taskbody>

    <!-- ================================================================ -->
    <!-- Abbreviations: periods in all-uppercase abbreviations            -->
    <!-- ================================================================ -->
    <prereq>
      <p>The U.S.A. data center is located on the east coast. Contact
         the N.A.T.O. liaison for access credentials.</p>

      <!-- CaseSensitiveTerms: wrong case for known terms -->
      <p>Send an IM to the support team for help. Use the Gimp
         application to edit screenshots before attaching them.</p>

      <!-- Conjunctions: starting sentences with conjunctions -->
      <p>And you must configure the firewall before proceeding.
         But the default settings are usually sufficient.
         Or you can use the automated configuration tool.
         So the process should complete in a few minutes.</p>

      <!-- ConsciousLanguage: non-inclusive terminology -->
      <p>Add the server to the blacklist to block incoming requests.
         Configure the master node to replicate data to the slave node.
         Update the whitelist to allow the new IP address.</p>

      <!-- Contractions: informal contractions in technical writing -->
      <p>You can't install the product without meeting prerequisites.
         It won't work if requirements aren't met. The installer
         doesn't support older platforms. If the process hasn't
         completed, don't restart the service. You shouldn't
         skip the verification step.</p>

      <!-- Definitions: acronyms not defined on first occurrence -->
      <p>Configure the LDAP server to authenticate users. Ensure that
         the RBAC policies are applied to the correct namespaces.
         Use the OIDC provider for single sign-on.</p>

      <!-- SentenceLength: sentences exceeding 32 words -->
      <p>The installation process for this particular product requires that
         you first verify that all of the necessary prerequisites have been
         properly configured and that the system meets all of the minimum
         requirements that are specified in the official documentation before
         you attempt to begin the installation process itself.</p>
    </prereq>

    <context>
      <!-- DoNotUseTerms: forbidden terms and phrases -->
      <p>Congratulations on completing the prerequisites. The product
         is basically ready to use out of the box. Use the foo variable
         to test connectivity. This approach is future-proof.</p>

      <!-- Ellipses: using ellipses in technical writing -->
      <p>Wait for the process to complete...</p>
      <p>The operation might take a few minutes…</p>

      <!-- EmDash: using em dashes instead of other punctuation -->
      <p>The installation process &#x2014; which takes several minutes &#x2014; requires
         network access. Review the logs &#x2014; they contain useful
         diagnostic information.</p>

      <!-- PassiveVoice: passive voice constructions -->
      <p>The software was installed by the administrator. The
         configuration file is updated automatically. Logs are
         generated by the monitoring agent. The certificate was
         renewed by the security team.</p>

      <!-- ProductCentricWriting: granting abilities to inanimate objects -->
      <p>The product allows you to manage containers efficiently.
         This feature enables you to automate deployments.
         The dashboard lets you monitor cluster health.
         The API permits users to create custom integrations.</p>

      <!-- SelfReferentialText: referring to the document itself -->
      <p>This topic provides an overview of the installation process.
         Later in this chapter, you can find advanced options.
         This module covers the prerequisites. Refer to this
         subsection for network configuration.</p>
    </context>

    <steps>
      <!-- TermsErrors: incorrect terms (error-level) -->
      <step>
        <cmd>Login to the web console and check the datacenter settings.</cmd>
        <!-- login: should be "log in" -->
        <info>
          <p>Make sure to setup the environment variables. Check the
             command line interface for additional options. Refer to
             a lot of documentation before proceeding.</p>
        </info>
      </step>

      <!-- Hyphens: incorrect hyphenation of compound terms -->
      <step>
        <cmd>Install the addon component for the meta-data service.</cmd>
        <info>
          <p>The hotswap feature allows you to replace hardware without
             downtime.</p>
        </info>
      </step>

      <!-- ObviousTerms: self-explanatory fields that do not need docs -->
      <step>
        <cmd>Enter your credentials in the Username field.</cmd>
        <info>
          <p>Type a secure passphrase in the Password field. Add your
             contact details in the Name field and the Mail field.</p>
        </info>
      </step>

      <!-- OxfordComma: missing Oxford comma in a series -->
      <step>
        <cmd>Select a target platform.</cmd>
        <info>
          <p>The product supports RHEL, Fedora and CentOS Stream.</p>
          <p>You can deploy on bare metal, virtual machines or cloud instances.</p>
        </info>
      </step>

      <!-- RepeatedWords: consecutive duplicate words -->
      <step>
        <cmd>Download the the installer package.</cmd>
        <info>
          <p>Check that that the file integrity is verified.</p>
        </info>
      </step>

      <!-- SimpleWords: complex words with simpler alternatives -->
      <step>
        <cmd>Endeavor to utilize the application accordingly.</cmd>
        <info>
          <p>Approximately ten nodes are sufficient for the cluster.
             The objective is to commence the migration promptly.</p>
        </info>
      </step>

      <!-- Slash: slashes between words instead of "and" or "or" -->
      <step>
        <cmd>Configure the read/write permissions for each user/group.</cmd>
        <info>
          <p>The admin/operator must verify the changes.</p>
        </info>
      </step>

      <!-- SmartQuotes: typographic quotation marks -->
      <step>
        <cmd>Click &#x201C;Install&#x201D; to begin the process.</cmd>
        <info>
          <p>Type &#x2018;yes&#x2019; at the confirmation prompt.</p>
        </info>
      </step>

      <!-- Spacing: double spaces between sentences -->
      <step>
        <cmd>Install the product.  Then configure it.</cmd>
        <info>
          <p>Restart the service after configuration.  Verify the
             installation.</p>
        </info>
      </step>

      <!-- Symbols: ampersand and exclamation mark outside of code -->
      <step>
        <cmd>Configure the input &amp; output settings.</cmd>
        <info>
          <p>Review logs &amp; metrics for errors. Important! Do not
             skip this step!</p>
        </info>
      </step>

      <!-- TermsWarnings: terms with preferred alternatives -->
      <step>
        <cmd>Use Ctrl-click to select multiple items.</cmd>
        <info>
          <p>Plan the transition from the legacy system. The button
             is in the bottom-left corner. Update the BIOS settings
             before proceeding. The bugfix is included in the latest
             release. Check that the configuration appears correctly
             etc.</p>
        </info>
      </step>

      <!-- TermsSuggestions: context-dependent alternatives -->
      <step>
        <cmd>Navigate to the settings page.</cmd>
        <info>
          <p>The page, which is located below the main dashboard,
             contains all configuration options. Deploy using k8s
             on the x64 platform. Afterwards, extract the tar file
             to the target directory.</p>
        </info>
      </step>
    </steps>

    <result>
      <!-- Multiple rules in a single paragraph -->
      <title>Common Mistakes To Avoid.</title>
      <!-- HeadingPunctuation + Headings (title case) combined -->
      <p>You can't login to the master node, and the whitelist
         isn't updated.  The the administrator was informed &amp;
         it basically allows you to fix it...</p>
    </result>

    <postreq>
      <!-- Code elements: these should NOT trigger rules -->
      <p>Run the following command:</p>
      <codeblock>$ blacklist --add server01 &amp;&amp; whitelist --remove server01
master_node="$(hostname)" # Don't flag contractions in code
setup_env foo bar  # setup and foo are fine in code</codeblock>
      <p>Set the <codeph>blacklist</codeph> option in the
         <filepath>/etc/master/config.yml</filepath> file.
         The <cmdname>setup</cmdname> command initializes the
         <varname>addon</varname> variable. Check the
         <systemoutput>can't connect</systemoutput> error message
         and the <option>--whitelist</option> flag.
         Use the <apiname>foo.bar</apiname> endpoint.</p>
    </postreq>
  </taskbody>
</task>
